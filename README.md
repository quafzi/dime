[![Build Status](https://travis-ci.org/quafzi/dime.png)](https://travis-ci.org/quafzi/dime)

Dime is a timetracking application, based on node.js and AngularJS.

Installation
============

Clone this repo, make a copy of `config/config.json.dist` named `config/config.json` and adjust the values in there.

Run your application by starting `node manage.js server`.
